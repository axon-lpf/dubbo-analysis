非常好，我们进入 **第九章：服务暴露机制设计**，在这一章我们将以前面完成的注册中心与网络通信为基础，**完成服务的暴露（即服务提供者启动时，自动注册服务，并监听请求）**。

---

# 第九章 服务暴露机制设计

> 完成服务提供者启动时的服务发布逻辑

---

## 🎯 9.1 本章目标

* 服务提供者在启动时完成以下事情：

    * 启动Netty服务端监听端口
    * 将服务注册到注册中心（如Zookeeper）
    * 把服务信息（接口与实现）暴露到内存中供查找
* 引入 **暴露器（Exporter）**、**服务仓库（ServiceRepository）** 等 Dubbo 风格核心组件

---

## 📁 9.2 项目结构更新

```
simple-dubbo/
├─ protocol/
│   ├─ RpcProtocol.java                # 暴露服务，封装暴露逻辑
│   └─ Exporter.java                   # 表示一个服务暴露点（可注销）
├─ registry/
│   └─ Registry.java
│   └─ ZookeeperRegistry.java
├─ remoting/
│   └─ NettyServer.java                # 服务端接入处理器
│   └─ NettyServerHandler.java
├─ provider/
│   └─ ServiceRepository.java          # 新增：服务仓库，记录已暴露服务
│   └─ ProviderBootstrap.java         # 服务启动入口
```

---

## ✅ 9.3 ServiceRepository：服务仓库

用于管理暴露出去的服务（接口和实现类的映射）

```java
package org.simple.rpc.provider;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

public class ServiceRepository {

    private static final Map<String, Object> serviceMap = new ConcurrentHashMap<>();

    public static void register(String interfaceName, Object refImpl) {
        serviceMap.put(interfaceName, refImpl);
    }

    public static Object getService(String interfaceName) {
        return serviceMap.get(interfaceName);
    }
}
```

---

## ✅ 9.4 Exporter 接口定义

```java
package org.simple.rpc.protocol;

public interface Exporter {
    void unexport();
}
```

---

## ✅ 9.5 RpcProtocol：服务暴露协议封装器

```java
package org.simple.rpc.protocol;

import org.simple.rpc.provider.ServiceRepository;
import org.simple.rpc.registry.Registry;

public class RpcProtocol {

    private final Registry registry;

    public RpcProtocol(Registry registry) {
        this.registry = registry;
    }

    /**
     * 暴露服务逻辑
     * @param interfaceName 接口名
     * @param impl 实现类
     * @param host 本地host
     * @param port 本地端口
     */
    public Exporter export(String interfaceName, Object impl, String host, int port) {
        // 1. 注册到服务仓库
        ServiceRepository.register(interfaceName, impl);

        // 2. 注册到注册中心
        String address = host + ":" + port;
        registry.register(interfaceName, address);

        // 3. 返回Exporter，用于后续可能注销服务
        return () -> {
            registry.unregister(interfaceName, address);
            System.out.println("服务下线: " + interfaceName);
        };
    }
}
```

---

## ✅ 9.6 NettyServerHandler 支持服务查找与执行

```java
package org.simple.rpc.remoting;

import io.netty.channel.ChannelHandlerContext;
import io.netty.channel.SimpleChannelInboundHandler;
import org.simple.rpc.core.codec.RpcRequest;
import org.simple.rpc.core.codec.RpcResponse;
import org.simple.rpc.provider.ServiceRepository;

import java.lang.reflect.Method;

public class NettyServerHandler extends SimpleChannelInboundHandler<RpcRequest> {

    @Override
    protected void channelRead0(ChannelHandlerContext ctx, RpcRequest request) {
        RpcResponse response = new RpcResponse();
        try {
            Object service = ServiceRepository.getService(request.getInterfaceName());
            if (service == null) throw new RuntimeException("未找到服务：" + request.getInterfaceName());

            Method method = service.getClass().getMethod(request.getMethodName(), request.getParamTypes());
            Object result = method.invoke(service, request.getParameters());
            response.setResult(result);
        } catch (Exception e) {
            response.setException(e);
        }
        ctx.writeAndFlush(response);
    }
}
```

---

## ✅ 9.7 ProviderBootstrap：服务端启动入口

```java
package org.simple.rpc.provider;

import org.simple.rpc.api.HelloService;
import org.simple.rpc.core.transport.NettyServer;
import org.simple.rpc.protocol.RpcProtocol;
import org.simple.rpc.registry.Registry;
import org.simple.rpc.registry.ZookeeperRegistry;

public class ProviderBootstrap {

    public static void main(String[] args) throws Exception {
        String zkAddress = "127.0.0.1:2181";
        int port = 20880;
        String host = "127.0.0.1";

        // 1. 启动Netty Server
        NettyServer.start(port);

        // 2. 创建注册中心
        Registry registry = new ZookeeperRegistry(zkAddress);

        // 3. 创建服务协议 & 暴露服务
        RpcProtocol protocol = new RpcProtocol(registry);
        protocol.export(HelloService.class.getName(), new HelloServiceImpl(), host, port);

        System.out.println("服务暴露成功: " + HelloService.class.getName());
    }
}
```

---

## ✅ 9.8 测试运行验证

* 启动 `ProviderBootstrap`，控制台输出：

```shell
Netty服务端已启动，端口:20880
服务已注册: org.simple.rpc.api.HelloService -> 127.0.0.1:20880
服务暴露成功: org.simple.rpc.api.HelloService
```

* 客户端从 Zookeeper 订阅服务，并完成调用。

---

## 📌 9.9 小结与预告

我们在本章实现了 Dubbo 服务暴露机制：

* **服务仓库**用于保存服务映射
* **Exporter/Protocol** 用于暴露服务并注册
* **服务启动流程整合**了注册中心与网络监听逻辑

**下一章（第十章）我们将实现消费端的引用机制：**

* 客户端根据接口名从注册中心订阅服务
* 构造远程调用代理，模拟 Dubbo 中的 `@DubboReference` 效果
* 初步封装 `Invoker` 进行解耦
