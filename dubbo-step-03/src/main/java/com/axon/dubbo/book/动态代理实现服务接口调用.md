收到，大飞哥！严格按你的目录，进入**第三章：动态代理实现服务接口调用**。

---

# 第三章 动态代理实现服务接口调用

---

## 3.1 JDK动态代理与CGLIB代理原理

### 3.1.1 为什么需要动态代理？

上一章，我们的客户端是手动写socket代码发请求的，**服务接口调用和底层通信强耦合**，不具备Dubbo的“接口透明调用”特性。而Dubbo的最大亮点就是让“服务消费者像调用本地方法一样调用远程服务”——这就要引入动态代理，将“网络通信细节”完全隐藏到代理对象背后。

### 3.1.2 JDK动态代理

* 适用于接口代理。
* 通过`java.lang.reflect.Proxy`和`InvocationHandler`实现。
* Dubbo中Consumer端默认就是用JDK Proxy实现远程接口代理。

### 3.1.3 CGLIB动态代理

* 适用于无接口的类代理（继承实现）。
* Dubbo中如果是没有接口的Bean或者部分特殊场景，会降级用CGLIB。

---

## 3.2 实现接口级RPC调用透明化

本章我们会**基于上一章代码基础**，引入JDK动态代理，让客户端不再直接写网络细节，而是像调用本地方法那样“RPC透明调用”远程接口。

---

### 3.2.1 项目结构演进

```
handwritten-dubbo
└── chapter03-proxy-rpc
    ├── api
    │   └── HelloService.java
    ├── provider
    │   └── ProviderServer.java
    │   └── HelloServiceImpl.java
    ├── rpc
    │   └── RpcInvocationHandler.java
    │   └── RpcClientProxy.java
    └── consumer
        └── ConsumerClient.java
```

---

### 3.2.2 核心代码详解

#### 1. 定义通用的RpcInvocationHandler

```java
// rpc/RpcInvocationHandler.java
package rpc;

import java.io.*;
import java.lang.reflect.InvocationHandler;
import java.lang.reflect.Method;
import java.net.Socket;

public class RpcInvocationHandler implements InvocationHandler {
    private final String host;
    private final int port;

    public RpcInvocationHandler(String host, int port) {
        this.host = host;
        this.port = port;
    }

    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        try (Socket socket = new Socket(host, port)) {
            ObjectOutputStream out = new ObjectOutputStream(socket.getOutputStream());
            ObjectInputStream in = new ObjectInputStream(socket.getInputStream());

            // 发送方法名和参数（只支持一个String参数的简单版本）
            out.writeObject(method.getName());
            out.writeObject(args[0]);
            out.flush();

            // 接收返回值
            Object result = in.readObject();
            return result;
        }
    }
}
```

#### 2. 动态代理工厂

```java
// rpc/RpcClientProxy.java
package rpc;

import java.lang.reflect.Proxy;

public class RpcClientProxy {
    private final String host;
    private final int port;

    public RpcClientProxy(String host, int port) {
        this.host = host;
        this.port = port;
    }

    @SuppressWarnings("unchecked")
    public <T> T getProxy(Class<T> interfaceClass) {
        return (T) Proxy.newProxyInstance(
            interfaceClass.getClassLoader(),
            new Class[]{interfaceClass},
            new RpcInvocationHandler(host, port)
        );
    }
}
```

#### 3. 服务端实现不变（与第二章相同）

```java
// provider/ProviderServer.java
// ...同第二章...
```

#### 4. 客户端透明调用

```java
// consumer/ConsumerClient.java
package consumer;

import api.HelloService;
import rpc.RpcClientProxy;

public class ConsumerClient {
    public static void main(String[] args) {
        RpcClientProxy proxy = new RpcClientProxy("localhost", 12345);
        HelloService helloService = proxy.getProxy(HelloService.class);

        // 关键：此处就像调用本地接口一样，无需关心网络细节！
        String result = helloService.sayHello("大飞哥");
        System.out.println("RPC调用结果：" + result);
    }
}
```

---

## 3.3 启动与验证

1. 先启动`ProviderServer`。
2. 再运行`ConsumerClient`，你会看到和本地方法调用一模一样的体验。

---

## 3.4 本章小结

* 本章让“RPC服务接口调用”完全本地化、透明化，和Dubbo源码的Invoker/Proxy实现模式一致。
* 后续章节会在此基础上，进一步引入协议抽象、可扩展Invoker、序列化等Dubbo核心设计思想。

---

**下一章预告**：
第四章将引入Netty作为网络传输核心，性能与并发能力大幅提升，同时工程结构逐步向Dubbo源码靠近！

---